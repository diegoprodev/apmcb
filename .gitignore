node_modules/
.claude/worktrees/

# Playwright test artifacts e logs MCP
test-results/
playwright-report/
apps/web/test-results/
apps/web/playwright-report/
.playwright-mcp/
# Playwright storageState (auth tokens — nunca commitar)
apps/web/.auth/
apps/bff/package-lock.json
.pnpm-store/
.next/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo
.turbo/
supabase/.temp/
.DS_Store
*.log
coverage/
.vscode/settings.json
.idea/

# Cloudflare — wrangler.toml contains plain-text vars (not secrets)
# but keep it out of git so we don't expose project IDs or non-secret config
wrangler.toml
supabase/.creds
