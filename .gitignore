node_modules/
.claude/worktrees/
.pnpm-store/
.next/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo
.turbo/
supabase/.temp/
.DS_Store
*.log
coverage/
.vscode/settings.json
.idea/

# Cloudflare — wrangler.toml contains plain-text vars (not secrets)
# but keep it out of git so we don't expose project IDs or non-secret config
wrangler.toml
